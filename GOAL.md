# GOAL — 서울 여행 웹앱

> 이 문서는 이 프로젝트의 판단 기준이다. 구현 중 선택이 갈리면 여기로 돌아온다.
> 결정을 바꾸려면 코드가 아니라 **이 문서를 먼저 고친다.**

---

## 0. 한 줄 정의

한국 공공데이터(TourAPI)를 기반으로 **외국인 여행자가 서울에서 무엇을 할지 정하고 하루 동선을 짜는** 웹앱. 한국인도 그대로 쓴다.

## 0.5 모토 — 이 앱의 정체성

> **공공데이터로 보여주지만, 나를 위한 앱이다.**

데이터는 공공이고 모두에게 같지만, **화면은 내 것처럼 느껴져야 한다.** 이 문장이 다른 모든 결정보다 위에 있다.

### 이건 구조 문제가 아니라 디자인 문제다

**카탈로그는 유지한다.** 이 앱은 여행 정보를 늘어놓는 카탈로그가 맞다. 그걸 부정하지 않는다.
바꿔야 하는 것은 **정보 구조가 아니라 제시 방식**이다. 같은 데이터가 기관의 목록으로도, 내 벽으로도 보인다.

레퍼런스가 이미 답을 갖고 있다. `docs/ref/IMG_3824.PNG` 의 binder_office 도 **카탈로그**다 —
포스터를 파는 목록이다. 그런데 검색 결과로 안 보이는 이유는 항목마다
**누군가의 방 벽에 걸린 액자로 찍혀 있기 때문**이다. 데이터는 같고 제시가 다르다.

| | 기관의 목록 (안 만든다) | 내 벽 (만든다) |
|---|---|---|
| 항목의 모습 | 썸네일 + 제목 + 설명 | **액자에 담긴 오브제** |
| 배치 | 균일한 3열 그리드 | **크기가 다른 액자가 걸린 벽** |
| 한 화면의 개수 | 20개, 페이지네이션 | **6~9개, 편집된 선택** |
| 개수 표시 | "3,412건 중 1–20" | **표시하지 않는다** |
| 문장의 주어 | "서울의 관광지" | **"당신의 화요일"** |
| 출처 표기 | 상단 배너 | **상세 화면 하단에만** |
| 밀도 | 높다 | **낮다. 여백이 소유감을 만든다** |

### 이 모토를 디자인으로 푸는 여섯 가지 장치

1. **액자(Frame).** 모든 스팟은 매트 + 안쪽 이미지의 이중 구조로 제시한다. 액자는 "벽에 걸린 내 것"을 뜻하고, 썸네일은 "데이터베이스의 한 행"을 뜻한다. 이 차이가 전부다
2. **벽(Wall).** 균일 그리드를 쓰지 않는다. 크기가 다른 액자를 비대칭으로 건다. 균일한 격자는 기계가 만든 것처럼 보이고, 불균일한 배치는 사람이 건 것처럼 보인다
3. **편집(Curation).** 한 화면에 6~9개만 놓는다. 총 건수·페이지 번호를 **표시하지 않는다**. "3,412건 중"이라는 문구 하나가 기관 느낌의 핵심이다
4. **목소리(Voice).** 캡션의 주어를 바꾼다. `Attractions in Jongno` 가 아니라 `Your Tuesday in Jongno`. i18n 메시지를 쓸 때부터 적용한다
5. **손때(Patina).** 본 것과 담은 것이 화면에 흔적으로 남는다. 기능이 아니라 **표시 방식**의 문제다 — 액자 매트 색이 미묘하게 달라지고, 담은 것에는 모서리 표식이 붙는다
6. **크롬 제거(No Portal Chrome).** 두꺼운 상단 내비, 브레드크럼, 화면을 가득 채운 필터 chip 나열, 제공기관 로고. 이것들이 "공공 포털"의 시각 언어다. 전부 쓰지 않는다

### 판정 기준

이 화면이 **누군가의 벽처럼 보이는가, 아니면 데이터베이스의 출력처럼 보이는가.**
갈리면 여백을 늘리고 개수를 줄인다.

## 1. 해결하려는 문제

외국인 여행자에게 부족한 건 **정보가 아니라 맥락**이다.

- 공공데이터에 장소는 다 있는데 "지금 여나 / 예약이 필요한가 / 영어가 통하나 / 현금만 받나"가 비어 있다
- 한국어 블로그는 접근이 안 되고, 영어 자료는 낡았다
- 지도 앱 라벨이 한글이라 읽지 못한다

**이 앱의 값어치는 번역이 아니라 빈칸 메우기와 동선 설계에 있다.** TourAPI 뷰어를 만드는 게 목표가 아니다.

---

## 2. 디자인 — 이 프로젝트에서 가장 중요한 축

### 2.1 절대 원칙

> **여행사 웹사이트처럼 만들지 않는다.**

금지 사항이다. 지키지 못하면 다른 게 다 맞아도 실패다.

- ❌ 텍스트 행을 세로로 쌓은 리스트
- ❌ 작은 썸네일 + 제목 + 설명 3줄 + 태그가 반복되는 카드
- ❌ 화면 상단을 가득 채운 필터 chip 나열
- ❌ 한 화면에 정보를 최대한 밀어넣기
- ❌ 파란 링크, 회색 테두리, 기본 그림자

- ✅ **이미지가 먼저다.** 텍스트는 이미지의 캡션이다
- ✅ 카드는 목록의 행이 아니라 **액자에 담긴 오브제**다
- ✅ 여백은 남는 공간이 아니라 **의도된 구성 요소**다
- ✅ 스크롤로 다 보여주는 대신 **진입**시킨다
- ✅ 선택지는 적고 크게
- ✅ 항목은 **액자**로 제시한다. 썸네일 + 제목 + 설명의 반복이 아니다
- ✅ 배치는 **비대칭**이다. 균일 격자는 기계가 만든 것처럼 보인다
- ❌ 총 건수·페이지 번호 표시 ("3,412건 중 1–20")
- ❌ 제공기관 로고를 첫 화면에 노출

### 2.2 레퍼런스 해석

`docs/ref/` 의 두 이미지에서 가져올 것:

**binder_office (아트 포스터 샵)**
- 순백이 아닌 **따뜻한 크림 바탕**
- 콘텐츠가 액자·프레임 안에 담긴 구조
- 채도 높은 색을 작은 점이 아니라 **큰 면**으로 씀
- 레트로 감성의 에디토리얼 세리프
- 대칭·중앙 정렬 구도

**connecto_studio (포토 스튜디오)**
- 옥상 트랙의 **민트 / 시안 / 코발트 / 핑크 색면 분할**
- 강한 자연광, 맑은 하늘, 높은 채도
- 기하학적으로 정리된 깨끗한 면
- 크림 바탕 + 중앙 정렬 세리프의 에디토리얼 카드

**공통분모 = 웨스 앤더슨:** 대칭 구도, 정면 촬영, 채도 높은 파스텔, 프레임 안의 프레임, 레트로 타이포.

### 2.3 컬러 토큰

바탕은 조용하게, 포인트는 과감하게. **회색조 UI를 만들고 색을 뿌리는 게 아니라, 크림 바탕 위에 색면을 놓는다.**

```
/* 바탕 — 순백(#FFFFFF)을 쓰지 않는다 */
--paper       #FAF6EF   기본 배경 (따뜻한 크림)
--paper-deep  #F2EBE0   섹션 교차, 카드 바탕
--ink         #1C1A17   본문 (따뜻한 먹색, 순검정 아님)
--ink-muted   #6B6459   보조 텍스트
--line        #E0D8CA   구분선 (회색 아님, 크림 계열)

/* 비비드 포인트 */
--cobalt      #1B4FD8   1차 액션, 링크
--cyan        #12B5C9   지도, 위치
--mint        #4FD1B0   보조
--coral       #FF5A46   강조, AI 추천
--pink        #F5A7C0   부드러운 강조
--mustard     #E8B33C   경고, 배지
```

**색 사용 규칙 (이게 핵심이다):**
1. **한 화면에는 지배적인 비비드 색이 하나뿐이다.** 나머지는 작은 표시로만 등장한다
2. 색은 **면으로** 쓴다. 1px 테두리나 작은 아이콘에만 쓰는 건 이 디자인이 아니다
3. 카테고리마다 색을 배정한다 (관광지=코발트, 음식=코랄, 문화=민트, 축제=머스터드). 사용자가 색으로 종류를 기억하게 한다
4. 그림자는 거의 쓰지 않는다. 깊이는 **색면의 대비**로 만든다

### 2.4 타이포그래피

- **디스플레이 (EN):** `Fraunces` — 레트로한 온기가 있는 가변 세리프. 웨스 앤더슨 감성의 핵심
- **디스플레이 (KO):** `Nanum Myeongjo` 또는 `Gowun Batang`
- **본문 (KO+EN 공용):** `Pretendard` — 한글·라틴 모두 안정적
- **라벨·메타:** 대문자 + 자간 넓힌 작은 산세리프

원칙: 제목은 크게, 본문은 짧게. **긴 문단을 쓰지 않는다.**

### 2.5 디자인 작업 방식 (강제)

UI를 만들 때 **반드시** 다음 스킬을 먼저 로드한다:

- `frontend-design` — 시각적 방향, 타이포, 템플릿 티 제거
- `supanova-premium-aesthetic` — 간격·그림자·카드 구조·한글 타이포 기준
- `supanova-design-engine` — 필요 시

기본값으로 대충 만들고 나중에 다듬지 않는다. **처음부터 디자인 시스템을 세우고 시작한다.**

---

## 3. 확정된 범위

| 축 | 결정 |
|---|---|
| 플랫폼 | 웹 (Next.js App Router). 모바일 앱 없음 |
| 위치 | `/Users/kim/workspace/tour` (스캐폴드 완료) |
| 지역 | **서울만**. 단 확장 가능한 구조 |
| 언어 | **영어·한국어·일본어·중국어(번체)·독일어·프랑스어 6개**. 기본 로케일 `en` |
| 주 타겟 | 방한 중 / 방한 직전 외국인 여행자 |

### v1 필수 기능

1. **지역·테마별 탐색** — 카테고리와 서울 구 단위 탐색 (단, 리스트 아님. §2.1 준수)
2. **지도 기반 주변 탐색** — 위치 반경 내 스팟
3. **AI 일정 추천** — 기간·관심사 → 동선 고려한 일자별 코스
4. **나만의 코스 저장** — 스팟 담기 + 일자별 편집 + 계정에 저장

### v1 제외 (명시적으로 하지 않는다)

- 예약·결제
- 사용자 후기·평점 작성
- 대중교통 실시간 길찾기 (외부 앱 딥링크로 대체)
- 서울 외 지역
- 영어·한국어 외 언어

---

## 4. 기술 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 프레임워크 | Next.js 16.3.1 + React 19.2.8 (설치됨) | |
| 언어 | TypeScript | |
| 스타일 | Tailwind CSS v4 (설치됨) | 토큰은 `@theme` 로 정의 |
| 패키지 매니저 | **pnpm 고정** | workspace 규칙 |
| i18n | 라우트 기반 `/en/...`, `/ko/...` | SEO. 검색 유입이 주 채널 |
| 지도 | **카카오맵 JS SDK** | 국내 POI·길찾기 품질. 바꾸려면 이 줄을 고친다 |
| DB·인증 | **Supabase** | 개인 여행루트 저장에 필요 |
| AI | **Claude API** 서버 라우트에서 호출 | |
| 배포 | Vercel | |

### ⚠️ Next.js 16 주의

`AGENTS.md` 가 경고한다 — **이 버전은 학습 데이터의 Next.js와 다르다.** 코드를 쓰기 전에 `node_modules/next/dist/docs/` 의 해당 가이드를 읽는다. 기억으로 쓰지 않는다.

### Supabase 설계

**인증 게이팅 (중요):**
- 탐색·지도·AI 추천·코스 구성은 **로그인 없이 전부 된다**
- 로그인은 **저장하는 순간에만** 요구한다
- 여행자에게 초기 가입 요구는 곧 이탈이다

**인증 수단:** Google OAuth + 이메일 매직 링크 (비밀번호 없음)

**테이블 초안:**
```
profiles     id(uuid, auth.users FK), locale, created_at
courses      id, user_id, title, start_date, days, is_public, share_slug
course_items id, course_id, day, order, content_id, content_type_id, memo
```
- RLS 필수. 본인 데이터만 접근
- `is_public` 인 코스는 `share_slug` 로 비로그인 열람 허용

### 데이터 소스

**TourAPI (한국관광공사, data.go.kr)**
- 국문/영문이 **별도 엔드포인트** → 로케일로 분기
- 서울 `areaCode = 1`
- `contentTypeId`: 12 관광지 / 14 문화시설 / 15 축제공연행사 / 25 여행코스 / 28 레포츠 / 32 숙박 / 38 쇼핑 / 39 음식점
- 필요 오퍼레이션: 지역기반 목록, 위치기반 목록, 키워드 검색, 공통 상세, 소개 상세, 이미지
- ⚠️ **엔드포인트 경로·버전 접미사는 착수 시 공식 문서로 확인한다.** 개편이 잦다. 기억으로 쓰지 않는다

### 지원 언어와 TourAPI 서비스 (실측)

언어마다 **별도 서비스**이고 각각 활용신청이 필요하다. 서비스명은 실제 호출로 확인했다 —
`JapService2`·`ChiService2` 같은 추측형 이름은 존재하지 않는다(코드 12).

| 로케일 | 서비스 | 상태 | 서울 전체 |
|---|---|---|---|
| `ko` | `KorService2` | 사용 가능 | 2,166 |
| `en` | `EngService2` | 사용 가능 | 714 |
| `ja` | `JpnService2` | 사용 가능 | 688 |
| `zh-Hant` | `ChtService2` | 사용 가능 | 506 |
| `fr` | `FreService2` | 사용 가능 | 276 |
| `de` | `GerService2` | 사용 가능 | 250 |
| `zh-Hans` | `ChsService2` | **활용신청 필요** (코드 30) | — |

**중국어 간체는 아직 못 켠다.** data.go.kr 에서 `ChsService2` 를 활용신청하면
`src/domain/shared/locale.ts` 의 `LOCALES` 에 한 줄, `tourapi-client.ts` 의 `SERVICE` 에 한 줄이면 켜진다.

**제목 형식이 언어마다 다르다.** 일본어는 전각 괄호를 쓰고(`スパレイ（스파레이）`),
프랑스어에는 번역 없이 한글만 오는 항목이 있다(`세종마을 음식문화거리`).
`parseSpotName` 이 두 경우를 모두 처리한다.

### ⚠️ 영문 서비스는 국문의 번역본이 아니다 (실측)

`.curvez/research/tourapi-english-coverage.md` 에서 **직접 호출로 확인한 사실**이다. 이 앱의 주 타겟이
외국인이므로 아래가 제품의 실제 제약이다.

| | 국문 (KorService2) | 영문 (EngService2) |
|---|---|---|
| `contentTypeId` | 12 / 14 / 15 / 28 / 32 / 38 / 39 | **75 / 76 / 78 / 79 / 80 / 82 / 85** (완전히 다르다) |
| `contentid` | — | **국문과 분리된 별도 공간.** 같은 장소도 ID 가 다르다 |
| 서울 관광지 | 418 | 405 (97%) — 단 **이미지 있는 것은 137건(34%)** |
| 서울 음식점 | 1007 | **66 (7%)** |
| 서울 전체 | 2166 | 714 (**33%**) |

**따라야 할 결론 세 가지**

1. **국문과 영문은 번역쌍이 아니라 독립된 두 카탈로그다.** 데이터 계층에서 그렇게 다룬다
2. **영문 로케일에서 이미지 없는 스팟은 목록에서 제외한다.** 관광지 137건이면 한 화면 9개 기준 15화면분이라 §0.5-3 "편집된 선택" 과 맞물린다
3. **상세 화면에서는 로케일 전환이 불가능하다.** `contentid` 가 분리돼 대응하는 반대 로케일 URL 을 만들 수 없다

**인증키:** 사용자가 `.env.local` 에 넣어준다. 그때까지는 mock 으로 개발하고, 어댑터 계층 뒤에 숨겨 플래그로 전환한다.

```
TOUR_API_KEY=              # 서버 전용. NEXT_PUBLIC_ 금지
NEXT_PUBLIC_KAKAO_MAP_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
USE_MOCK_DATA=true
```

---

## 5. 설계 원칙 (판단이 갈릴 때의 기준)

0. **나를 위한 앱이다 (§0.5).** 판단이 갈리면 "이 화면이 나에게 맞춰져 있는가"를 먼저 묻는다. 다른 원칙과 충돌하면 이것이 이긴다
1. **영어가 1급 시민이다.** 한국어를 만들고 번역하는 게 아니다. 영어 UX를 먼저 설계한다
2. **한국어 이름을 항상 병기한다.** 택시 기사에게 보여주고 표지판과 대조해야 한다. 영문명만 있는 화면은 현장에서 쓸모없다
3. **데이터 빈칸을 숨기지 않는다.** 영업시간이 없으면 "정보 없음"이라 쓰고 공식 링크를 준다
4. **모바일 우선.** 실사용은 여행 중, 한 손, 야외, 느린 로밍이다
5. **지도는 UI로 덮는다.** 카카오 타일이 한글이므로 마커·바텀시트·카드는 반드시 영문을 담는다
6. **AI 추천은 근거를 보여준다.** 왜 이 순서인지(거리·영업시간)를 문장으로 노출한다
7. **TourAPI 는 서버에서 호출한다.** 인증키가 클라이언트 번들에 절대 들어가지 않는다

---

## 6. 완료 기준

**디자인**
- [ ] §2.3 토큰이 Tailwind `@theme` 에 정의되고 하드코딩 hex 가 코드에 없다
- [ ] 어느 화면도 "텍스트 행 리스트"가 아니다 (§2.1)
- [ ] 배경에 `#FFFFFF` 가 쓰이지 않는다
- [ ] 카테고리별 색 배정이 전 화면에서 일관된다
- [ ] 모든 스팟이 액자(매트 + 안쪽 이미지) 구조로 제시된다 (§0.5)
- [ ] 목록 화면의 그리드가 비대칭이다 — 같은 크기 카드의 균일 반복이 없다
- [ ] 한 화면에 스팟이 6~9개다. 총 건수·페이지 번호가 화면에 없다
- [ ] UI 문구의 주어가 "서울의 X"가 아니라 "당신의 X"다
- [ ] 본 스팟과 담은 스팟이 액자 위에 시각적 흔적으로 남는다
- [ ] 제공기관 표기가 상세 화면 하단에만 있다

**기능**
- [ ] `/en`, `/ko` 가 동작한다
- [ ] 홈·탐색·지도에서 언어 전환 시 현재 페이지가 유지된다
- [ ] **상세 화면의 언어 전환은 목록으로 보낸다.** `contentid` 가 로케일 간 분리돼 있어 같은 스팟으로 이어갈 수 없다 — 조용히 실패하지 말고 그렇게 동작한다고 알린다
- [ ] 영문 로케일 목록에 이미지 없는 스팟이 나오지 않는다
- [ ] 카테고리 + 구 단위로 스팟을 탐색한다
- [ ] 상세에서 사진·설명·주소(영문+한글 병기)·영업시간·공식 링크를 본다
- [ ] 지도에 반경 내 스팟이 뜨고 마커 탭 시 카드가 열린다
- [ ] 기간·관심사 입력 → 일자별 코스 생성, 스팟마다 선정 근거 표시
- [ ] 로그인 없이 코스를 구성하고, 저장 시점에만 로그인을 요구한다
- [ ] 저장한 코스가 다른 기기에서 보인다
- [ ] 공개 코스가 `share_slug` 로 비로그인 열람된다

**품질**
- [ ] mock ↔ 실데이터가 환경변수만으로 전환된다
- [ ] `TOUR_API_KEY` 가 클라이언트 번들에 없다 (빌드 산출물 grep 으로 확인)
- [ ] 375px 에서 가로 스크롤이 없다
- [ ] Supabase RLS 로 타인 코스 접근이 차단된다 (실제로 시도해 확인)
- [ ] `pnpm build` 통과

---

## 7. 작업 순서

1. **디자인 시스템** — 스킬 로드 → 토큰·타이포·핵심 컴포넌트 확정
2. **뼈대** — i18n 라우팅, 레이아웃, 폰트
3. **데이터 계층** — TourAPI 타입·어댑터·mock·서버 라우트·캐싱
4. **탐색** — 목록(§2.1 준수)·필터·상세
5. **지도** — 카카오맵, 위치기반 조회, 마커·바텀시트
6. **Supabase** — 스키마·RLS·인증·코스 CRUD
7. **AI 추천** — Claude API 라우트, 프롬프트, 근거 노출 UI
8. **마감** — 실데이터 전환, 성능, 접근성, 배포

---

## 8. 열린 질문

- 서울 구 필터를 `sigunguCode` 로 할지 좌표로 계산할지
- AI 추천에 후보 스팟을 몇 개까지 넘길지 (토큰 비용 vs 품질)
- 영문 데이터가 빈 스팟을 목록에서 뺄지, 한글로라도 보여줄지
- "여행사스럽지 않은 탐색 UI"의 구체적 형태 — 지도 우선 / 풀블리드 카드 / 큐레이션 진입 중 무엇으로 갈지는 1단계 디자인에서 확정한다
