# 팀 편성 — 브랜드 네이밍 회의

승인 시각: 20260825-002750 (+0900)
승인 내용: 사용자가 구성안을 "승인 — 이대로 시작" 으로 선택. 기존 브랜드 MARU 는 확정이 아니라
후보 풀의 한 항목(`by: user`)으로 취급해 같은 기준표로 평가한다.

## 팀 명단과 소유 경로

| 워커 | 역할 | 소유 경로 | 비고 |
|---|---|---|---|
| curvez-marketer | 리딩 — 브랜드 코어·브리프·평가·A/B 수렴 | `.curvez/brand/` 아래만 | 핸드오프는 `.curvez/handoff/curvez-marketer.<ts>.json` |
| curvez-designer | 참가자 — 시각·발음 관점 후보 | 이번 회의에서는 파일 쓰기 없음 | 후보는 핸드오프 `decisions[]` 로만 반환 |
| curvez-nextjs | 참가자 — 코드 식별자·도메인 관점 후보 | 이번 회의에서는 파일 쓰기 없음 | 후보는 핸드오프 `decisions[]` 로만 반환 |
| curvez-requirements | 참가자 — 제품 정의 관점 후보 | 이번 회의에서는 파일 쓰기 없음 | 후보는 핸드오프 `decisions[]` 로만 반환 |

curvez-react-native 는 제외 — `.curvez/profile.json` 의 `stack` 이 `nextjs` 이고 `paths.mobile` 이 없다.

## 라운드 구성

| 라운드 | 워커 | 병렬/순차 | 임무 | 결과 |
|---|---|---|---|---|
| R1 | curvez-marketer 단독 | 순차 | 브랜드 코어 + 브리프 + 참가자 후보 요청 | 완료 (20260825-003259) |
| R2 | curvez-designer ∥ curvez-nextjs ∥ curvez-requirements | 병렬 3명 | 후보 3개 이상씩 반환 | 완료 (후보 10 + MARU 노트) |
| R3 | curvez-marketer 재개 | 순차 | 평가 + 충돌 실검색 → A/B (Todam/Nunchi) | 완료 — 사용자가 A/B 모두 거부 |
| R4 | curvez-marketer 재개 | 순차 | 사용자 피드백(토속·식품 연상 결격, 의성어·의태어 허용)으로 브리프 개정 + 재소집 요청 | 완료 (20260825-010824) |
| R5 | 참가자 3명 재개 | 병렬 3명 | 개정 브리프 기준 후보 재제출 | 완료 (20260825-011255~011713) |
| R6 | curvez-marketer 재개 | 순차 | 재평가 → 새 A/B (Jabak/Chagok) | 완료 — 사용자 거부 |
| R7 | curvez-marketer 단독 | 순차 | 감성 축·외국어 허용·리서치 기반 재수렴 → A/B (Mirinae/Mangata) | 완료 — 사용자가 새 방향 제시 |
| R8 | curvez-marketer 단독 | 순차 | 사용자 제안 패턴 "life is OOO" 평가 + 빈칸 후보 개발 + 실검색 → 최종 A/B | 완료 — **사용자 A 선택, `Life is Nearby` 확정.** 종결 핸드오프 20260825-234710 (`status: done`) |

동시 워커 최대 3명(상한 5 이내). **재소집 상한 1회 — R4~R6 으로 소진.** 최종 A/B 선택은 사용자.

## 재소집 승인 근거 (무변경 재실행 예외)

사용자가 20260825 A/B(Todam/Nunchi)를 거부하며 기준 피드백을 직접 제시 — "토담은 된장 브랜드
같다"(토속·식품 연상 결격), "의성어·의태어도 괜찮다"(어원 범위 확장). R4~R6 은 승인된 구성
(마케터 + 참가자 3명)과 구성원·소유권·병렬 판정이 동일한 무변경 재실행이므로 재승인 예외에 해당한다.

## 병렬 판정 근거

- R2 참가자 3명은 이번 라운드에서 소유 경로에 파일을 쓰지 않는다. 산출물은 핸드오프
  `decisions[]` 뿐이고, `.curvez/handoff/` 는 파일명이 `<from>.<timestamp>.json` 이라 충돌하지 않는다 → 병렬 가능.
- R1·R3 의 마케터는 `.curvez/brand/` 를 단독 소유하므로 겹칠 상대가 없다.
- `paths.domain` 없음(단일 Next.js 저장소) — 구현 에이전트 2종 동시 실행 케이스 아님.

## 실행 회고 점수 — tour 네이밍 런 (2026-08-25)

이 점수는 Curvez 플러그인 하네스 자체의 종합점수가 아니다. 그 정식 평가는
`/Users/kim/.claude/plugins/marketplaces/curvez/result.html`의 고정 8개 평가축을 따른다.
여기서는 현재 보존된 핸드오프 17개와 R1~R8 실행표를 근거로 **tour 네이밍 런 한 건의 운영 품질**만
회고한다. 네이밍 결과의 취향이나 후보 점수와도 별개다.

**실행 회고 점수: 82/100 — B. 감사 가능성은 높지만, 이 런의 수렴 비용과 상태 동기화는 개선이 필요하다.**

| 평가 축 | 배점 | 점수 | 근거 |
|---|---:|---:|---|
| 작업 분해·역할 소유권 | 20 | 18 | 리더와 참가자의 책임, 쓰기 경로, 순차/병렬 경계가 사전에 명시됐다. 마케터만 `.curvez/brand/`를 쓰고 참가자는 핸드오프만 쓰게 해 충돌 범위를 좁혔다. 다만 R7·R8의 단독 전환 기준은 최초 계획에 없고 사후 추가됐다. |
| 병렬 실행·충돌 격리 | 15 | 14 | R2·R5에서 서로 독립적인 세 관점을 병렬 실행했고, 타임스탬프 파일명으로 결과 충돌을 피했다. 실제 소유 경로 충돌 기록은 없다. 병렬 이득을 시간·토큰으로 계측하지 않아 만점은 아니다. |
| 핸드오프 계약·감사 가능성 | 20 | 19 | 17/17 파일이 필수 8필드(`from`, `to`, `status`, `summary`, `artifacts`, `decisions`, `blocked_on`, `verification`)를 갖췄다. 결정 120건 중 113건(94.2%)에 `reversible_at`이 있고, 검증 110건 중 107건(97.3%)이 통과했다. 실패 3건도 원인과 후속 통과를 숨기지 않고 기록했다. |
| 상태 관리·재개 정확성 | 15 | 11 | `done`·`partial`·`blocked`를 구분하고 재개 입력을 남겼다. R4·R5가 완료 후에도 각각 `진행`·`대기`로 남았고, R8 완료 전에는 `naming.md`와 이 문서의 현재 라운드가 갈렸다. 지금은 수동으로 맞췄지만 단일 상태 정본이 없다는 증거다. |
| 피드백 반영·의사결정 품질 | 15 | 13 | 사용자 거부를 후보 실패로만 보지 않고 기준표 결함까지 역추적했으며, 자기 오류와 반대 근거를 기록했다. 그러나 향토·식품 연상 실패가 Todam→Jabak으로 반복돼 사용자 연상 검증이 에이전트 평가 뒤에 놓였다. |
| 효율·종료 조건 | 15 | 7 | 마케터 핸드오프 6개가 모두 `blocked`로 끝났고, 후보가 44개까지 늘었지만 선택에 이르지 못했다. 재소집 상한은 있었으나 새 단독 라운드가 계속 추가됐고, 취향 축 질문이 늦어 외부 충돌 검색과 재평가가 반복됐다. |
| **합계** | **100** | **82** | **강한 기록 시스템, 약한 수렴 제어.** |

### 실측 요약

- 핸드오프 17개: `done` 7 · `partial` 4 · `blocked` 6
- 에이전트 6종: architect 2 · designer 3 · marketer 6 · nextjs 2 · requirements 2 · researcher 2
- 결정 120건, 되돌림 위치 명시 113건(94.2%)
- 검증 110건, 통과 107건(97.3%)
- `blocked_on` 누적 61건 — 위험·후속 메모와 실제 진행 차단 조건이 한 배열에 섞여 수가 부풀었다
- 병렬 웨이브 2회(R2·R5), 각 3명. 기록상 파일 충돌 0건

### 다음 버전의 필수 개선

1. **P0 — 취향 게이트를 첫 라운드 앞에 둔다.** 선호 브랜드 2~3개, 거부 브랜드 2~3개,
   한국어/외국어·구상/추상·기능/감정 축을 먼저 받기 전에는 후보 생성 에이전트를 소집하지 않는다.
2. **P0 — 상태 정본을 하나로 만든다.** `current_round`, `status`, `depends_on`, `last_handoff`를 가진
   기계 판독 가능한 manifest에서 라운드 표와 문서 머리말을 생성한다. 사람이 세 곳을 따로 고치지 않는다.
3. **P1 — 핸드오프에 `run_id`, `round_id`, `parent_handoff`, `input_hash`를 넣는다.** 지금은 파일명과
   본문을 읽어야 어느 실행의 후속인지 복원할 수 있다.
4. **P1 — `blocked_on`을 분리한다.** 지금 당장 진행을 막는 `blocking`과 나중에 확인할 `risk`·`follow_up`을
   별도 필드로 나눠, 61건의 누적 메모가 실제 차단 상태처럼 보이지 않게 한다.
5. **P1 — 종료 조건을 결과가 아니라 비용에도 건다.** 두 번 연속 거부되거나 새 근거 비율이 일정 값보다
   낮으면 재검색을 멈추고 사용자 선호 인터뷰로 전환한다. 새 라운드 이름을 붙이는 것으로 상한을 우회하지 않는다.
6. **P2 — 검증을 델타 기반으로 캐시한다.** 도메인·npm·앱스토어·상표 조회는 신규 후보와 만료된 결과만
   다시 검사하고, 동일 후보의 기존 결과는 확인일과 TTL로 재사용한다.

### 이 점수를 하네스 종합점수로 쓰면 안 되는 이유

- 정식 보고서의 8개 축·가중치 대신 이 런에 맞춘 6개 축을 썼다.
- 정식 평가의 부트스트랩·플러그인 통합·훅·공식 CLI 검증을 점수에 넣지 않았다.
- 사용자 선택 미완료와 반복 라운드를 실행 효율에서 크게 감점했다. 이는 하네스 구조와 특정 네이밍
  전략의 품질이 섞인 값이다.
- 따라서 82와 정식 보고서의 점수를 증감 비교하면 안 된다. 82는 회고 지표이고, 하네스 기준선은
  기존 92점에서 실제 실행 근거를 반영한 현재 93점이다.

### 실행 회고 판정

이번 실행에서 확인된 하네스의 강점은 **누가 무엇을 왜 결정했고 어디서 되돌릴 수 있는지 남기는 능력**이다. 약점은
**좋은 기록을 많이 만드는 동안 사용자 선택에 빨리 도달하지 못하는 것**이다. 다음 개선의 우선순위는
에이전트를 더 늘리는 것이 아니라, 소집 전 취향 게이트와 단일 상태 머신을 두는 것이다.
