import type { Coordinate } from "@/domain/spot/coordinate";

/**
 * 날씨를 물어볼 기본 지점 — 서울시청.
 *
 * 이 앱의 벽은 지금 서울만 건다. 사용자 위치를 묻지 않는 이유는 두 가지다.
 * 첫째, 위치 권한 팝업은 첫 화면에서 가장 비싼 방해다. 둘째, 여행자가 알고 싶은
 * 것은 "내가 지금 선 자리"가 아니라 **"오늘 그 도시가 어떤가"** 다.
 *
 * 조회 자체는 좌표를 받으므로, 지역이 늘면 고른 지역의 좌표를 넘기면 된다.
 * 그때 이 상수는 "아무것도 고르지 않았을 때"의 값으로 남는다.
 */
export const DEFAULT_WEATHER_POINT: Coordinate = { lng: 126.9779, lat: 37.5663 };
